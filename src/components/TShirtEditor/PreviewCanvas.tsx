import { StaticCanvas } from "fabric";
import React, { useRef } from "react";

const PreviewCanvas = () => {
  return <canvas id="preview-canvas" width={80} height={80} />;
};

export default PreviewCanvas;
